[
  {
    "note": "04 — Operator Overloading",
    "questions": [
      {
        "question": "Which operators MUST be member functions?",
        "options": [
          "+, -, *",
          "=, [], (), ->",
          "==, !=, <",
          ">>, <<"
        ],
        "correctAnswer": "=, [], (), ->",
        "explanation": "Note: '=, [], (), -> MUST be members. Others typically non-member for symmetry.'"
      },
      {
        "question": "What does <=> (C++20) generate?",
        "options": [
          "Just ==",
          "All six: <, <=, >, >=, ==, !=",
          "Just < and >",
          "Only ordering"
        ],
        "correctAnswer": "All six: <, <=, >, >=, ==, !=",
        "explanation": "'One operator gives ALL comparisons. Generates ==, !=, <, <=, >, >= from member comparison.'"
      },
      {
        "question": "Why should commutative operators be non-members?",
        "options": [
          "Faster",
          "Symmetry — both operands treated equally for implicit conversions",
          "Required by standard",
          "Only non-members work"
        ],
        "correctAnswer": "Symmetry — both operands treated equally for implicit conversions",
        "explanation": "Non-member operator+(T, T) allows implicit conversion on both sides. Member only converts right operand."
      },
      {
        "question": "Why shouldn't you overload && and ||?",
        "options": [
          "Not allowed",
          "They lose short-circuit evaluation when overloaded",
          "Too slow",
          "Deprecated"
        ],
        "correctAnswer": "They lose short-circuit evaluation when overloaded",
        "explanation": "Built-in && and || short-circuit. Overloaded versions evaluate ALL arguments before the function call."
      },
      {
        "question": "What does the spaceship operator return?",
        "options": [
          "bool",
          "std::strong_ordering, std::partial_ordering, or std::weak_ordering",
          "int",
          "float"
        ],
        "correctAnswer": "std::strong_ordering, std::partial_ordering, or std::weak_ordering",
        "explanation": "<=> returns a comparison category type. The compiler uses it to synthesize <, <=, >, >= from the ordering."
      }
    ]
  },
  {
    "note": "05 — Templates & Concepts",
    "questions": [
      {
        "question": "When are templates instantiated?",
        "options": [
          "At link time",
          "At compile time, at point of use — compiler generates copy per type",
          "At runtime",
          "During preprocessing"
        ],
        "correctAnswer": "At compile time, at point of use — compiler generates copy per type",
        "explanation": "Templates instantiated at point of use. Compiler needs full definition visible (why templates go in headers)."
      },
      {
        "question": "What problem do C++20 concepts solve?",
        "options": [
          "Slow compilation",
          "Incomprehensible error messages — concepts produce clean errors at call site",
          "Binary size",
          "Runtime"
        ],
        "correctAnswer": "Incomprehensible error messages — concepts produce clean errors at call site",
        "explanation": "'The biggest template problem: 300 lines of error trace. Concepts: clean message at the call site.'"
      },
      {
        "question": "What is a variadic template?",
        "options": [
          "Template with one parameter",
          "Template accepting any number of type/argument parameters using ...",
          "Template with default args",
          "Template with concepts"
        ],
        "correctAnswer": "Template accepting any number of type/argument parameters using ...",
        "explanation": "'Args... is a parameter pack — captures any number of types. Fold expressions (C++17) operate on packs.'"
      },
      {
        "question": "What does if constexpr do?",
        "options": [
          "Runtime if",
          "Compile-time branch — discarded branch doesn't even need to compile for given type",
          "Same as regular if",
          "Optimization hint"
        ],
        "correctAnswer": "Compile-time branch — discarded branch doesn't even need to compile for given type",
        "explanation": "if constexpr evaluates condition at compile time. Discarded branch is completely eliminated."
      },
      {
        "question": "Why must template code go in headers?",
        "options": [
          "Style convention",
          "Compiler needs full definition at instantiation point — can't link against pre-compiled template",
          "Faster compilation",
          "Required by standard"
        ],
        "correctAnswer": "Compiler needs full definition at instantiation point — can't link against pre-compiled template",
        "explanation": "Templates are instantiated at use. The definition must be visible. Put in headers or .inl files."
      }
    ]
  },
  {
    "note": "01 — Move Semantics & Perfect Forwarding",
    "questions": [
      {
        "question": "What does std::move actually do?",
        "options": [
          "Moves the object",
          "Casts to rvalue reference (T&&), enabling move semantics",
          "Deletes original",
          "Copies efficiently"
        ],
        "correctAnswer": "Casts to rvalue reference (T&&), enabling move semantics",
        "explanation": "'std::move is a cast. It doesn't move anything — it enables the move constructor to be selected.'"
      },
      {
        "question": "What's the difference between std::move and std::forward?",
        "options": [
          "None",
          "move unconditionally casts to rvalue; forward conditionally casts preserving value category",
          "forward slower",
          "move for unique_ptr"
        ],
        "correctAnswer": "move unconditionally casts to rvalue; forward conditionally casts preserving value category",
        "explanation": "move = always rvalue. forward = lvalue stays lvalue, rvalue stays rvalue (perfect forwarding)."
      },
      {
        "question": "What is an rvalue reference (T&&)?",
        "options": [
          "Reference to const",
          "Reference that binds to temporaries — enables moving from them",
          "Same as T&",
          "Pointer to T"
        ],
        "correctAnswer": "Reference that binds to temporaries — enables moving from them",
        "explanation": "T&& binds to rvalues (temporaries, std::move results). Key to move semantics: steal resources, don't copy."
      },
      {
        "question": "What does a moved-from object contain?",
        "options": [
          "Original data",
          "Valid but unspecified state — typically empty/null/zero",
          "Garbage",
          "nullptr always"
        ],
        "correctAnswer": "Valid but unspecified state — typically empty/null/zero",
        "explanation": "Moved-from objects are in a valid-but-unspecified state. Can be destroyed or assigned to. Don't rely on specific values."
      },
      {
        "question": "What is guaranteed about a moved-from std::unique_ptr?",
        "options": [
          "It still owns the object",
          "It is nullptr — the moved-to pointer now owns the object",
          "It's in an unspecified state",
          "It throws on access"
        ],
        "correctAnswer": "It is nullptr — the moved-to pointer now owns the object",
        "explanation": "unique_ptr guarantees moved-from state is nullptr. Other types are in valid-but-unspecified state."
      }
    ]
  },
  {
    "note": "02 — constexpr, consteval & constinit",
    "questions": [
      {
        "question": "What's the difference between const and constexpr?",
        "options": [
          "None",
          "const = runtime promise (won't modify); constexpr = compile-time evaluation guaranteed",
          "const faster",
          "constexpr deprecated"
        ],
        "correctAnswer": "const = runtime promise (won't modify); constexpr = compile-time evaluation guaranteed",
        "explanation": "'const promises won't write. constexpr guarantees value known at compile time.'"
      },
      {
        "question": "What does consteval enforce?",
        "options": [
          "Compile or runtime",
          "MUST be compile-time — error if called with runtime arguments",
          "Runtime only",
          "Lazy"
        ],
        "correctAnswer": "MUST be compile-time — error if called with runtime arguments",
        "explanation": "'consteval = immediate function. Every call MUST produce a compile-time constant.'"
      },
      {
        "question": "What does constinit (C++20) do?",
        "options": [
          "Same as constexpr",
          "Compile-time init, runtime mutable — solves static init order fiasco",
          "Prevents mutation",
          "Thread-local"
        ],
        "correctAnswer": "Compile-time init, runtime mutable — solves static init order fiasco",
        "explanation": "constinit: initialized at compile time but mutable at runtime. No guard variable overhead."
      },
      {
        "question": "Can a constexpr function be called with runtime arguments?",
        "options": [
          "No",
          "Yes — it behaves like a normal function at runtime",
          "Only in C++20",
          "Only with consteval"
        ],
        "correctAnswer": "Yes — it behaves like a normal function at runtime",
        "explanation": "constexpr functions CAN be evaluated at compile time. With runtime args, they're normal functions."
      },
      {
        "question": "Where does a constexpr variable's value live at runtime?",
        "options": [
          "On the heap",
          "In .rodata (read-only data segment) or as immediate value in instructions",
          "On the stack",
          "In a register only"
        ],
        "correctAnswer": "In .rodata (read-only data segment) or as immediate value in instructions",
        "explanation": "constexpr variables are compile-time constants. Compiler can embed them as immediates or place in .rodata."
      }
    ]
  },
  {
    "note": "03 — noexcept & Move Semantics",
    "questions": [
      {
        "question": "Why does noexcept move constructor improve vector performance?",
        "options": [
          "Doesn't",
          "vector uses move_if_noexcept: noexcept→move(pointer swap); non-noexcept→copy for exception safety",
          "Enables SIMD",
          "Binary size"
        ],
        "correctAnswer": "vector uses move_if_noexcept: noexcept→move(pointer swap); non-noexcept→copy for exception safety",
        "explanation": "'vector checks noexcept. Yes → move (~3 cycles). No → copy (thousands of cycles).'"
      },
      {
        "question": "What happens if a noexcept function throws?",
        "options": [
          "Caught by caller",
          "std::terminate() — instant death, no recovery",
          "Stack unwinds normally",
          "Compiler error"
        ],
        "correctAnswer": "std::terminate() — instant death, no recovery",
        "explanation": "noexcept is a promise you cannot break. Violation = std::terminate(), no stack unwinding."
      },
      {
        "question": "When should a function be marked noexcept?",
        "options": [
          "Always",
          "Move constructors, destructors, swap — operations that should never fail",
          "Never",
          "Only in templates"
        ],
        "correctAnswer": "Move constructors, destructors, swap — operations that should never fail",
        "explanation": "Move ctors and dtors should always be noexcept. Swaps should be. There's no reason for 'stealing guts' to throw."
      },
      {
        "question": "What does the noexcept operator do?",
        "options": [
          "Marks a function noexcept",
          "Compile-time check: noexcept(expr) returns true if expr is noexcept",
          "Executes expr",
          "Catches exceptions"
        ],
        "correctAnswer": "Compile-time check: noexcept(expr) returns true if expr is noexcept",
        "explanation": "noexcept(expression) is a compile-time operator. Returns bool. Used in templates for conditional noexcept."
      },
      {
        "question": "Why are destructors implicitly noexcept?",
        "options": [
          "Performance only",
          "Throwing from destructor during stack unwinding = std::terminate() — double exception",
          "Style",
          "Required by standard"
        ],
        "correctAnswer": "Throwing from destructor during stack unwinding = std::terminate() — double exception",
        "explanation": "If a destructor throws while another exception is in flight, the program terminates. noexcept prevents this."
      }
    ]
  },
  {
    "note": "04 — std variant, optional & any",
    "questions": [
      {
        "question": "Key difference between variant and C union?",
        "options": [
          "None",
          "variant has discriminator tracking active alternative — type-safe; C unions don't",
          "Slower",
          "On heap"
        ],
        "correctAnswer": "variant has discriminator tracking active alternative — type-safe; C unions don't",
        "explanation": "variant stores max(sizeof(alternatives)) + discriminator. Always knows which type is active."
      },
      {
        "question": "Does std::optional allocate on heap?",
        "options": [
          "Yes",
          "No — inline storage, sizeof ≈ sizeof(T) + alignment padding",
          "Only for large T",
          "Depends"
        ],
        "correctAnswer": "No — inline storage, sizeof ≈ sizeof(T) + alignment padding",
        "explanation": "'optional is NOT a pointer. No heap allocation. Stores value inline.' sizeof(optional<int>) ≈ 8."
      },
      {
        "question": "What is std::visit used for?",
        "options": [
          "Heap allocation",
          "Compile-time exhaustive dispatch over all variant alternatives",
          "Thread safety",
          "Serialization"
        ],
        "correctAnswer": "Compile-time exhaustive dispatch over all variant alternatives",
        "explanation": "visit generates dispatch table. If you add a new alternative, visit won't compile until handled. Exhaustive."
      },
      {
        "question": "When should you use std::any?",
        "options": [
          "Always",
          "When types aren't enumerable at compile time — scripting, plugin APIs",
          "Never",
          "For small types only"
        ],
        "correctAnswer": "When types aren't enumerable at compile time — scripting, plugin APIs",
        "explanation": "any uses heap + RTTI. Prefer variant for closed type sets, optional for nullable values."
      },
      {
        "question": "What does std::optional::value_or(default) do?",
        "options": [
          "Always returns the contained value",
          "Returns contained value if present, otherwise returns the default",
          "Throws if empty",
          "Modifies the optional"
        ],
        "correctAnswer": "Returns contained value if present, otherwise returns the default",
        "explanation": "value_or() is the safe accessor: get the value or a fallback without checking or throwing."
      }
    ]
  },
  {
    "note": "05 — Coroutines (C++20)",
    "questions": [
      {
        "question": "What three keywords identify C++20 coroutines?",
        "correctAnswer": "co_await, co_yield, co_return",
        "acceptableAnswers": [
          "co_await co_yield co_return",
          "co_await, co_yield, co_return"
        ],
        "explanation": "co_await suspends/resumes, co_yield produces value and suspends, co_return completes the coroutine."
      },
      {
        "question": "What is a coroutine?",
        "options": [
          "A thread",
          "A function that can suspend and resume — like a function with multiple entry/exit points",
          "A lambda",
          "A template"
        ],
        "correctAnswer": "A function that can suspend and resume — like a function with multiple entry/exit points",
        "explanation": "Coroutines can pause mid-execution and resume later. Enables cooperative multitasking without threads."
      },
      {
        "question": "What does co_yield do?",
        "options": [
          "Ends the coroutine",
          "Produces a value and suspends — for generators",
          "Throws",
          "Calls another coroutine"
        ],
        "correctAnswer": "Produces a value and suspends — for generators",
        "explanation": "co_yield returns a value to the caller and suspends. The coroutine can be resumed to yield more values."
      },
      {
        "question": "What does co_await do?",
        "options": [
          "Ends",
          "Suspends execution until the awaited operation completes",
          "Starts a thread",
          "Throws"
        ],
        "correctAnswer": "Suspends execution until the awaited operation completes",
        "explanation": "co_await is the primary coroutine keyword: suspend until the awaitable produces a result."
      },
      {
        "question": "Can C++ coroutines run on multiple threads?",
        "options": [
          "No, single-threaded only",
          "They can — the coroutine itself is stackless; resumption can happen on any thread",
          "Always multithreaded",
          "Only with co_await"
        ],
        "correctAnswer": "They can — the coroutine itself is stackless; resumption can happen on any thread",
        "explanation": "C++20 coroutines are stackless. The coroutine frame is heap-allocated. Resumption can happen from any thread."
      }
    ]
  },
  {
    "note": "06 — std ranges (C++20)",
    "questions": [
      {
        "question": "Key advantage of ranges over iterator pairs?",
        "options": [
          "Faster",
          "Composable pipelines with lazy evaluation — filter|transform|take, zero intermediate allocations",
          "Smaller binary",
          "Better errors"
        ],
        "correctAnswer": "Composable pipelines with lazy evaluation — filter|transform|take, zero intermediate allocations",
        "explanation": "'Ranges compose with |. filter | transform | take — lazy, no allocations. Zero-cost abstraction.'"
      },
      {
        "question": "What is a view in ranges?",
        "options": [
          "A copy of data",
          "A lazy, non-owning range adaptor — computes on-demand, no storage",
          "A container",
          "An iterator"
        ],
        "correctAnswer": "A lazy, non-owning range adaptor — computes on-demand, no storage",
        "explanation": "Views are lightweight objects storing reference to underlying range + parameters. Pull model."
      },
      {
        "question": "What does rv::filter(pred) return?",
        "options": [
          "A new vector",
          "A view — lazy, no allocation, filters on-demand",
          "The original container",
          "An iterator"
        ],
        "correctAnswer": "A view — lazy, no allocation, filters on-demand",
        "explanation": "filter returns a view. No work happens until iteration. Elements are filtered one at a time on-demand."
      },
      {
        "question": "What does rv::transform(fn) do?",
        "options": [
          "Modifies in place",
          "Returns a view applying fn to each element on-demand",
          "Copies everything",
          "Sorts"
        ],
        "correctAnswer": "Returns a view applying fn to each element on-demand",
        "explanation": "transform is lazy. The function is applied as elements are pulled through the pipeline."
      },
      {
        "question": "What is a projection in std::ranges algorithms?",
        "options": [
          "A 3D transform",
          "A unary function applied to each element before comparison/operation",
          "A sorting criteria",
          "A view"
        ],
        "correctAnswer": "A unary function applied to each element before comparison/operation",
        "explanation": "Projections: sort(players, {}, &Player::score) applies &Player::score before comparing. Eliminates 90% of custom comparator lambdas."
      }
    ]
  },
  {
    "note": "07 — C++23 Overview",
    "questions": [
      {
        "question": "What does deducing this eliminate?",
        "options": [
          "Virtual functions",
          "The CRTP pattern — no more static_cast<Derived*>(this)",
          "Templates",
          "Move semantics"
        ],
        "correctAnswer": "The CRTP pattern — no more static_cast<Derived*>(this)",
        "explanation": "Note: 'Deducing this kills the CRTP pattern. void f(this Self&& self) deduces the derived type automatically.'"
      },
      {
        "question": "What is std::expected<T,E>?",
        "options": [
          "Optional with error",
          "Rust-style Result — holds either T (value) or E (error), forces error handling",
          "Smart pointer",
          "Coroutine type"
        ],
        "correctAnswer": "Rust-style Result — holds either T (value) or E (error), forces error handling",
        "explanation": "expected<T,E> = value OR error. Monadic operations: and_then, transform, or_else. Replaces pair<T,bool> pattern."
      },
      {
        "question": "What monadic operation chains std::optional calls without nested ifs?",
        "options": [
          "optional::get()",
          "optional::and_then() and optional::transform()",
          "optional::value()",
          "optional::reset()"
        ],
        "correctAnswer": "optional::and_then() and optional::transform()",
        "explanation": "C++23 adds and_then (chain), transform (map), or_else (fallback). Eliminates nested if-checking."
      },
      {
        "question": "What does std::flat_map use internally instead of a tree?",
        "options": [
          "Hash table",
          "Sorted contiguous arrays (vectors) — binary search, cache-friendly",
          "Linked list",
          "B-tree"
        ],
        "correctAnswer": "Sorted contiguous arrays (vectors) — binary search, cache-friendly",
        "explanation": "flat_map stores keys+values in sorted vectors. O(log n) binary search, cache-friendly iteration. Inserts are O(n)."
      },
      {
        "question": "What does multidimensional operator[] (C++23) enable?",
        "options": [
          "Multiple return values",
          "grid[x, y, z] — multiple arguments to operator[]",
          "Parallel array access",
          "SIMD operations"
        ],
        "correctAnswer": "grid[x, y, z] — multiple arguments to operator[]",
        "explanation": "Before C++23: operator()(x,y,z) or tuple. C++23: operator[](x,y,z) natively."
      },
      {
        "question": "What does if consteval detect?",
        "options": [
          "Exception context",
          "Whether code is executing at compile time vs runtime",
          "Template instantiation",
          "Optimization level"
        ],
        "correctAnswer": "Whether code is executing at compile time vs runtime",
        "explanation": "if consteval: branch taken only during compile-time evaluation. Different algorithms for compile vs runtime."
      },
      {
        "question": "What does std::println provide over std::cout << std::format?",
        "options": [
          "Nothing new",
          "Direct formatted output with newline — no intermediate string allocation, compile-time checked",
          "Faster runtime",
          "Only for debugging"
        ],
        "correctAnswer": "Direct formatted output with newline — no intermediate string allocation, compile-time checked",
        "explanation": "std::println writes directly to stream buffer. No intermediate std::string. Compile-time format string validation."
      },
      {
        "question": "Why is C++23 called a 'completion' release?",
        "options": [
          "It's the last C++ release",
          "It polishes C++20 features and fills gaps rather than introducing paradigm shifts",
          "It focuses only on performance",
          "It deprecates old features"
        ],
        "correctAnswer": "It polishes C++20 features and fills gaps rather than introducing paradigm shifts",
        "explanation": "Note: 'C++23 is a completion release — it polishes features from C++20, fills long-standing gaps.'"
      }
    ]
  },
  {
    "note": "01 — C++ vs UE",
    "questions": [
      {
        "question": "Why does UE disable C++ exceptions?",
        "options": [
          "Too slow",
          "Performance predictability, code size, and console platform constraints",
          "Windows incompatibility",
          "Deprecated in C++20"
        ],
        "correctAnswer": "Performance predictability, code size, and console platform constraints",
        "explanation": "Exceptions add 5-15% binary size and make control flow unpredictable. UE uses macros instead."
      },
      {
        "question": "What does check(expr) do?",
        "options": [
          "Logs a warning",
          "If false → crashes immediately with message + callstack — in dev AND shipping builds",
          "Throws exception",
          "Returns false"
        ],
        "correctAnswer": "If false → crashes immediately with message + callstack — in dev AND shipping builds",
        "explanation": "check() is fatal in all builds. ensure() logs and continues (dev only). verify() like check but expr runs in shipping too."
      },
      {
        "question": "How does UE's Cast<T>() work without RTTI?",
        "options": [
          "It doesn't — UE uses dynamic_cast",
          "Uses compile-time static class flags generated by UHT — O(1) type check",
          "Falls back to reinterpret_cast",
          "Uses virtual functions"
        ],
        "correctAnswer": "Uses compile-time static class flags generated by UHT — O(1) type check",
        "explanation": "UHT generates StaticClass() and TCastFlags. Cast<T>() compares flags — no vtable walk, no RTTI overhead."
      },
      {
        "question": "What UE type replaces std::string?",
        "options": [
          "TCHAR",
          "FString",
          "FName",
          "FText"
        ],
        "correctAnswer": "FString",
        "explanation": "FString = mutable heap string (like std::string). FName = interned string (O(1) comparison). FText = localizable."
      },
      {
        "question": "What is the difference between FName and FString?",
        "options": [
          "No difference",
          "FName is an interned, immutable string — O(1) comparison via index; FString is a regular mutable string",
          "FName is shorter",
          "FName is deprecated"
        ],
        "correctAnswer": "FName is an interned, immutable string — O(1) comparison via index; FString is a regular mutable string",
        "explanation": "FName stores strings in a global table. Comparing two FNames is comparing integers (table indices)."
      },
      {
        "question": "Why can't you use raw AActor* to keep a UObject alive in UE?",
        "options": [
          "It works fine",
          "Only UPROPERTY() members and TObjectPtr prevent GC — raw pointers don't count as references",
          "UE doesn't use pointers",
          "AActor is abstract"
        ],
        "correctAnswer": "Only UPROPERTY() members and TObjectPtr prevent GC — raw pointers don't count as references",
        "explanation": "Note: 'A raw AActor* does NOT prevent garbage collection. Only UPROPERTY() and TObjectPtr<T> keep UObjects alive.'"
      },
      {
        "question": "What UE type replaces std::shared_ptr for non-UObject types?",
        "options": [
          "TObjectPtr",
          "TSharedPtr",
          "TUniquePtr",
          "FSharedRef"
        ],
        "correctAnswer": "TSharedPtr",
        "explanation": "TSharedPtr is reference-counted like shared_ptr but for non-UObject types. TObjectPtr is for UObject GC tracking."
      },
      {
        "question": "What does ensure() do differently from check()?",
        "options": [
          "Same thing",
          "Logs error + callstack but continues execution — only in development builds, stripped from shipping",
          "Crashes harder",
          "Only works in debugger"
        ],
        "correctAnswer": "Logs error + callstack but continues execution — only in development builds, stripped from shipping",
        "explanation": "ensure() is for non-fatal errors you want to know about during development without crashing. Stripped from shipping."
      }
    ]
  },
  {
    "note": "02 — From Source to Running",
    "questions": [
      {
        "question": "What are the four compilation stages in order?",
        "correctAnswer": "Preprocessor, Compiler, Assembler, Linker",
        "acceptableAnswers": [
          "preprocessor compiler assembler linker",
          "Preprocessor → Compiler → Assembler → Linker"
        ],
        "explanation": ".cpp → Preprocessor (.i) → Compiler (.s) → Assembler (.o) → Linker (.exe)."
      },
      {
        "question": "What does the preprocessor do with #include?",
        "options": [
          "Import only used declarations",
          "Copy-pastes the entire file at the directive position",
          "Links to a shared library",
          "Checks file existence"
        ],
        "correctAnswer": "Copy-pastes the entire file at the directive position",
        "explanation": "#include = text paste. <iostream> adds ~25,000 lines before the compiler sees it."
      },
      {
        "question": "What does the linker resolve?",
        "options": [
          "Syntax errors",
          "Symbols — connects 'require' in one .o to 'provide' in another .o or library",
          "Includes",
          "Template instantiation"
        ],
        "correctAnswer": "Symbols — connects 'require' in one .o to 'provide' in another .o or library",
        "explanation": "Each .o has a symbol table: what it provides (defines) and what it requires (declared but not defined). Linker matches them."
      },
      {
        "question": "What does the OS loader do before main() runs?",
        "options": [
          "Nothing",
          "Creates process with private virtual address space, maps .text/.data/.bss/stack/heap, loads dynamic libs",
          "Compiles the program",
          "Runs the garbage collector"
        ],
        "correctAnswer": "Creates process with private virtual address space, maps .text/.data/.bss/stack/heap, loads dynamic libs",
        "explanation": "Loader: create process → map segments → load dynamic libraries → jump to _start → __libc_start_main → main()."
      },
      {
        "question": "What file extension does the assembler output?",
        "options": [
          ".cpp",
          ".i",
          ".s",
          ".o / .obj"
        ],
        "correctAnswer": ".o / .obj",
        "explanation": "Assembler: .s (assembly text) → .o (binary machine code + symbol table + relocation entries)."
      },
      {
        "question": "What does the compiler's optimizer do?",
        "options": [
          "Just formats code",
          "Inlining, dead code elimination, loop unrolling, vectorization — transforms IR to faster equivalent",
          "Links libraries",
          "Handles templates"
        ],
        "correctAnswer": "Inlining, dead code elimination, loop unrolling, vectorization — transforms IR to faster equivalent",
        "explanation": "Compiler: C++ → AST → IR (GIMPLE/LLVM IR) → optimized IR → target assembly. Optimization happens at IR level."
      },
      {
        "question": "What is an 'undefined reference' linker error?",
        "options": [
          "Syntax error in code",
          "A symbol was declared but never defined — the linker can't find its .o or library",
          "Missing #include",
          "Wrong compiler version"
        ],
        "correctAnswer": "A symbol was declared but never defined — the linker can't find its .o or library",
        "explanation": "Undefined reference = linker found a 'require' with no matching 'provide'. Forgot to link a .cpp or library."
      }
    ]
  },
  {
    "note": "03 — Modules (C++20)",
    "questions": [
      {
        "question": "What is the primary advantage of modules over #include?",
        "options": [
          "Faster runtime",
          "Dramatically faster compilation — modules are pre-compiled once, not text-pasted into every file",
          "Smaller binary",
          "Easier syntax"
        ],
        "correctAnswer": "Dramatically faster compilation — modules are pre-compiled once, not text-pasted into every file",
        "explanation": "import std; replaces pasting the entire standard library. Modules parsed once, used many times."
      },
      {
        "question": "What does 'export' do in a module?",
        "options": [
          "Nothing special",
          "Makes a function/type/namespace visible to importers — without export, it's internal",
          "Optimizes the code",
          "Enables templates"
        ],
        "correctAnswer": "Makes a function/type/namespace visible to importers — without export, it's internal",
        "explanation": "Only exported entities are accessible to consumers. Non-exported = module-private."
      },
      {
        "question": "What is a module partition?",
        "options": [
          "A separate module",
          "A way to split a large module internally — consumers import the primary module and see everything",
          "A deprecated feature",
          "A header file"
        ],
        "correctAnswer": "A way to split a large module internally — consumers import the primary module and see everything",
        "explanation": "Partitions (module mymath:basics) are internal organization. Consumers only import mymath."
      },
      {
        "question": "What is a header unit?",
        "options": [
          "A special kind of module",
          "A pre-compiled header imported as module: import <iostream>; — migration bridge from #include",
          "A C++23 feature only",
          "A deprecated pattern"
        ],
        "correctAnswer": "A pre-compiled header imported as module: import <iostream>; — migration bridge from #include",
        "explanation": "Header units let you import existing headers as modules. Not as clean as named modules (macro leakage possible)."
      },
      {
        "question": "Why doesn't UE use modules yet?",
        "options": [
          "They don't work",
          "Compiler support stabilized only recently; UBT needs changes; BMI not portable; massive codebase",
          "Modules are too slow",
          "Epic doesn't like C++20"
        ],
        "correctAnswer": "Compiler support stabilized only recently; UBT needs changes; BMI not portable; massive codebase",
        "explanation": "Four reasons: unstable compiler support until 2022-23, UBT changes needed, BMI format not portable, 20-year codebase."
      }
    ]
  },
  {
    "note": "04 — Exceptions & Error Handling",
    "questions": [
      {
        "question": "What is stack unwinding?",
        "options": [
          "A compiler optimization",
          "When an exception is thrown, destructors of all local objects are called in reverse order as the stack backtracks to find a catch handler",
          "Memory cleanup",
          "Function inlining"
        ],
        "correctAnswer": "When an exception is thrown, destructors of all local objects are called in reverse order as the stack backtracks to find a catch handler",
        "explanation": "Each frame's cleanup runs destructors in reverse construction order. Exception tables map PC addresses to cleanup actions."
      },
      {
        "question": "Why should you always catch by const reference?",
        "options": [
          "Performance",
          "Avoids slicing — catching by value would copy and potentially slice derived exception types",
          "Required by the standard",
          "It's just convention"
        ],
        "correctAnswer": "Avoids slicing — catching by value would copy and potentially slice derived exception types",
        "explanation": "catch (const std::exception& e) preserves the actual exception type. catch (std::exception e) slices it."
      },
      {
        "question": "What are the three exception safety guarantee levels?",
        "correctAnswer": "Basic (no leaks), Strong (commit-or-rollback), No-throw (cannot fail)",
        "acceptableAnswers": [
          "basic strong no-throw",
          "basic, strong, and no-throw",
          "Basic Strong No-throw"
        ],
        "explanation": "Basic: no leaks but state may change. Strong: succeeds OR state unchanged. No-throw: never fails (destructors, swap)."
      },
      {
        "question": "What happens if a destructor throws during stack unwinding?",
        "options": [
          "It's caught normally",
          "std::terminate() — double exception is fatal, program dies immediately",
          "The exception is ignored",
          "The program continues"
        ],
        "correctAnswer": "std::terminate() — double exception is fatal, program dies immediately",
        "explanation": "Throwing from destructor during another exception = two active exceptions → std::terminate(). Destructors must be noexcept."
      },
      {
        "question": "How does the runtime know which destructors to call during unwinding?",
        "options": [
          "It guesses",
          "Exception tables (.eh_frame on Linux, .pdata/.xdata on Windows) map every PC address to cleanup actions",
          "It walks the call stack manually",
          "The compiler inserts try/catch everywhere"
        ],
        "correctAnswer": "Exception tables (.eh_frame on Linux, .pdata/.xdata on Windows) map every PC address to cleanup actions",
        "explanation": "The compiler generates unwind tables. At runtime, the exception machinery uses these tables to find and execute cleanup code."
      }
    ]
  },
  {
    "note": "05 — CMake & Build Systems",
    "questions": [
      {
        "question": "What is CMake?",
        "options": [
          "A compiler",
          "A meta-build system — generates native build files (Makefiles, VS solutions) from CMakeLists.txt",
          "A linker",
          "A package manager"
        ],
        "correctAnswer": "A meta-build system — generates native build files (Makefiles, VS solutions) from CMakeLists.txt",
        "explanation": "CMake reads CMakeLists.txt and outputs platform-specific build files. It doesn't build directly."
      },
      {
        "question": "Why should you build out-of-source?",
        "options": [
          "It's faster",
          "Keeps generated files separate from source — can delete build/ and regenerate cleanly",
          "Required by CMake",
          "For version control"
        ],
        "correctAnswer": "Keeps generated files separate from source — can delete build/ and regenerate cleanly",
        "explanation": "mkdir build && cd build && cmake .. — all generated files in build/. Never build in the source directory."
      },
      {
        "question": "What does target_link_libraries do?",
        "options": [
          "Copies libraries",
          "Specifies that a target depends on another library — linker will resolve symbols from it",
          "Downloads libraries",
          "Creates a library"
        ],
        "correctAnswer": "Specifies that a target depends on another library — linker will resolve symbols from it",
        "explanation": "target_link_libraries(MyGame PRIVATE Engine) tells CMake MyGame needs Engine. PRIVATE = only MyGame needs it."
      },
      {
        "question": "What is the difference between STATIC and SHARED libraries?",
        "options": [
          "No difference",
          "STATIC = copied into executable at link time; SHARED = loaded at runtime (.dll/.so)",
          "STATIC is faster",
          "SHARED is deprecated"
        ],
        "correctAnswer": "STATIC = copied into executable at link time; SHARED = loaded at runtime (.dll/.so)",
        "explanation": "add_library(Engine STATIC ...) vs SHARED. Static increases binary size; shared reduces it but adds runtime dependency."
      },
      {
        "question": "What does CMake's configure step do?",
        "options": [
          "Compiles",
          "Generates build files — reads CMakeLists.txt, detects compiler, finds dependencies, creates Makefiles/sln",
          "Links",
          "Runs tests"
        ],
        "correctAnswer": "Generates build files — reads CMakeLists.txt, detects compiler, finds dependencies, creates Makefiles/sln",
        "explanation": "cmake .. is configure. cmake --build . is build. They're separate steps — reconfigure only when CMakeLists.txt changes."
      }
    ]
  }
]