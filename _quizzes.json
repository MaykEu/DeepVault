[
  {
    "note": "01 — Stack vs Heap",
    "questions": [
      {
        "question": "What is the fundamental performance difference between stack and heap allocation?",
        "options": [
          "Same speed",
          "Stack: one CPU instruction (move stack pointer); heap: searching for free space, ~50-200+ cycles",
          "Heap is faster",
          "Stack is slower due to LIFO"
        ],
        "correctAnswer": "Stack: one CPU instruction (move stack pointer); heap: searching for free space, ~50-200+ cycles",
        "explanation": "Note states stack allocation is automatic, one CPU instruction. Heap involves searching for free space."
      },
      {
        "question": "What determines the lifetime of a stack variable?",
        "options": [
          "Until delete",
          "Program duration",
          "Tied to function scope — destroyed when the function returns",
          "Until the next allocation"
        ],
        "correctAnswer": "Tied to function scope — destroyed when the function returns",
        "explanation": "Stack lifetime is automatic — popped when function returns. Heap: manual via delete."
      },
      {
        "question": "What is the typical maximum stack size?",
        "options": [
          "Unlimited",
          "1-8 MB (platform-dependent)",
          "Available RAM (gigabytes)",
          "64 KB"
        ],
        "correctAnswer": "1-8 MB (platform-dependent)",
        "explanation": "Stack ~1-8 MB. Heap: gigabytes. Large data must go on the heap."
      },
      {
        "question": "What register tracks the top of the stack on x86-64?",
        "options": [
          "RAX",
          "RBP",
          "RSP",
          "RIP"
        ],
        "correctAnswer": "RSP",
        "explanation": "Note: 'The CPU tracks the top with a register called the stack pointer (RSP on x86-64).'"
      },
      {
        "question": "Why is the stack cache-friendly?",
        "options": [
          "It uses a special cache",
          "It's LIFO — the top is always in L1/L2 cache, recently used memory is hot",
          "It preloads data",
          "It bypasses cache"
        ],
        "correctAnswer": "It's LIFO — the top is always in L1/L2 cache, recently used memory is hot",
        "explanation": "The stack's LIFO nature means the most recently pushed data sits at the top, which stays cache-hot."
      }
    ]
  },
  {
    "note": "02 — Object Memory Layout",
    "questions": [
      {
        "question": "Why is sizeof(struct{char a; int b; char c;}) typically 12, not 6?",
        "options": [
          "Vtable pointer",
          "Padding — CPU requires aligned access; compiler inserts invisible bytes",
          "Hidden members",
          "sizeof rounds up"
        ],
        "correctAnswer": "Padding — CPU requires aligned access; compiler inserts invisible bytes",
        "explanation": "Note: 'Six bytes of data, twelve bytes of object. Padding. A 4-byte int must live at an address divisible by 4.'"
      },
      {
        "question": "What hardware constraint drives alignment requirements?",
        "options": [
          "OS memory manager",
          "CPUs read memory in aligned word chunks — misaligned data crossing a word boundary requires two reads",
          "C++ standard mandates it",
          "Compiler optimization only"
        ],
        "correctAnswer": "CPUs read memory in aligned word chunks — misaligned data crossing a word boundary requires two reads",
        "explanation": "Note: CPUs don't read byte-by-byte. A misaligned int crossing a word boundary needs two memory accesses."
      },
      {
        "question": "How can you reduce padding in a struct?",
        "options": [
          "Use smaller types only",
          "Order members from largest to smallest alignment",
          "Use #pragma pack everywhere",
          "Add more members"
        ],
        "correctAnswer": "Order members from largest to smallest alignment",
        "explanation": "Placing larger-alignment members first reduces padding gaps between members."
      },
      {
        "question": "What is the alignment of a double on x86-64?",
        "options": [
          "4 bytes",
          "8 bytes",
          "16 bytes",
          "1 byte"
        ],
        "correctAnswer": "8 bytes",
        "explanation": "Note: 'A type of size N must live at an address divisible by N (for fundamental types up to pointer size).' double is 8 bytes."
      },
      {
        "question": "What does AoS vs SoA stand for and which is faster for SIMD?",
        "options": [
          "Allocation of Structures vs Structures of Allocation",
          "Array of Structures vs Structure of Arrays — SoA is often faster for SIMD",
          "Access or Store",
          "None of the above"
        ],
        "correctAnswer": "Array of Structures vs Structure of Arrays — SoA is often faster for SIMD",
        "explanation": "Structure of Arrays keeps same-type fields contiguous for better cache and SIMD utilization."
      }
    ]
  },
  {
    "note": "03 — Smart Pointers & Ownership",
    "questions": [
      {
        "question": "Why don't raw pointers auto-clean heap memory when they go out of scope?",
        "options": [
          "Compiler optimization",
          "The pointer variable (8 bytes holding the address) is destroyed, but the heap object it pointed to is NOT",
          "C++ limitation",
          "OS handles it"
        ],
        "correctAnswer": "The pointer variable (8 bytes holding the address) is destroyed, but the heap object it pointed to is NOT",
        "explanation": "Note: 'When a pointer goes out of scope, the pointer VARIABLE is destroyed. But the memory it points to on the heap is NOT.'"
      },
      {
        "question": "What RAII principle do smart pointers implement?",
        "options": [
          "Manual new/delete",
          "Tie resource lifetime to object lifetime — destructor automatically frees the managed object",
          "Reference counting everything",
          "Stack allocation for all data"
        ],
        "correctAnswer": "Tie resource lifetime to object lifetime — destructor automatically frees the managed object",
        "explanation": "Smart pointer destructor automatically deletes the managed object when the smart pointer itself goes out of scope."
      },
      {
        "question": "What is unique about std::unique_ptr ownership?",
        "options": [
          "Multiple pointers can share ownership",
          "Only one unique_ptr can own the object at a time — can't be copied, only moved",
          "It uses reference counting",
          "It can only point to stack objects"
        ],
        "correctAnswer": "Only one unique_ptr can own the object at a time — can't be copied, only moved",
        "explanation": "unique_ptr enforces exclusive ownership. Copy is deleted; only move is allowed."
      },
      {
        "question": "When should you use std::shared_ptr over std::unique_ptr?",
        "options": [
          "Always — it's safer",
          "When multiple owners need to share the same object, and the last one to die cleans it up",
          "Never — unique_ptr is always better",
          "Only in multithreaded code"
        ],
        "correctAnswer": "When multiple owners need to share the same object, and the last one to die cleans it up",
        "explanation": "shared_ptr uses reference counting. Object deleted when the last shared_ptr is destroyed."
      },
      {
        "question": "What is std::weak_ptr used for?",
        "options": [
          "A weaker version of unique_ptr",
          "Breaking reference cycles in shared_ptr graphs — it doesn't affect the reference count",
          "Replacing raw pointers everywhere",
          "Thread-safe weak references"
        ],
        "correctAnswer": "Breaking reference cycles in shared_ptr graphs — it doesn't affect the reference count",
        "explanation": "weak_ptr observes a shared_ptr's object without owning it. Essential for breaking circular references."
      }
    ]
  },
  {
    "note": "04 — RAII & The Rule of Five",
    "questions": [
      {
        "question": "What does RAII stand for?",
        "correctAnswer": "Resource Acquisition Is Initialization",
        "acceptableAnswers": [
          "Resource Acquisition Is Initialization",
          "RAII"
        ],
        "explanation": "Note: 'RAII means tie the lifetime of a resource to the lifetime of an object. Constructor acquires, destructor releases.'"
      },
      {
        "question": "What are the five special member functions in the Rule of Five?",
        "correctAnswer": "Destructor, copy constructor, copy assignment, move constructor, move assignment",
        "acceptableAnswers": [
          "destructor copy constructor copy assignment move constructor move assignment",
          "~T(), T(const T&), T& operator=(const T&), T(T&&), T& operator=(T&&)"
        ],
        "explanation": "Rule of Five: if you define any of the five, you should explicitly define all of them for consistent resource management."
      },
      {
        "question": "Why does the Rule of Five exist?",
        "options": [
          "Backward compatibility",
          "If you need a custom destructor (resource ownership), compiler-generated copy/move might do shallow copies of your resource",
          "Compiler optimization mandate",
          "C++ standard committee preference"
        ],
        "correctAnswer": "If you need a custom destructor (resource ownership), compiler-generated copy/move might do shallow copies of your resource",
        "explanation": "A custom destructor signals resource ownership. Default copy/move would copy the pointer, not the resource — double-free or leak."
      },
      {
        "question": "What is the Rule of Zero?",
        "options": [
          "Don't use any special members",
          "Don't declare any special members — use RAII types (unique_ptr, vector, string) as members so compiler-generated ones are correct",
          "Always =default everything",
          "Zero-overhead principle"
        ],
        "correctAnswer": "Don't declare any special members — use RAII types (unique_ptr, vector, string) as members so compiler-generated ones are correct",
        "explanation": "Rule of Zero: if all members are RAII types, the compiler-generated destructor/copy/move are already correct."
      },
      {
        "question": "In what three situations are destructors guaranteed to run in C++?",
        "correctAnswer": "Normal scope exit, exception unwinding, and early return/break/continue",
        "acceptableAnswers": [
          "scope exit, exception, early return",
          "normal exit exception early return/break"
        ],
        "explanation": "Note lists three guarantees: normal scope exit, exception unwinding (stack unwinding), and any control flow leaving scope."
      }
    ]
  },
  {
    "note": "10 — std format (C++20)",
    "questions": [
      {
        "question": "What is the key safety advantage of std::format over printf?",
        "options": [
          "Faster",
          "Format string type mismatches are caught at compile time, not runtime",
          "More format specifiers",
          "Less memory usage"
        ],
        "correctAnswer": "Format string type mismatches are caught at compile time, not runtime",
        "explanation": "Note: 'Errors in your format string are caught at compile time — not at 3 AM when a player triggers a rare dialogue path.'"
      },
      {
        "question": "How do you make a custom type work with std::format?",
        "options": [
          "Overload operator<<",
          "Specialize std::formatter<T> with constexpr parse() and format() methods",
          "Inherit from std::formattable",
          "Use a registration macro"
        ],
        "correctAnswer": "Specialize std::formatter<T> with constexpr parse() and format() methods",
        "explanation": "Note: 'Custom types integrate via template specialization of std::formatter<T> with a constexpr parse() and a format() method.'"
      }
    ]
  },
  {
    "note": "05 — std array & C Arrays",
    "questions": [
      {
        "question": "What is the key advantage of std::array over a C-style array?",
        "options": [
          "Stored on heap",
          "Same memory layout but adds .size(), .at() bounds checking, and never decays to pointer",
          "Always SIMD-optimized",
          "Automatic bounds checking on every operator[]"
        ],
        "correctAnswer": "Same memory layout but adds .size(), .at() bounds checking, and never decays to pointer",
        "explanation": "std::array is zero-overhead: same contiguous memory as C array, but with container interface."
      },
      {
        "question": "What happens to a C array's size information when passed to a function?",
        "options": [
          "It's preserved",
          "It decays to a pointer — sizeof becomes pointer size (8 bytes), array size is lost",
          "It's passed as a hidden second parameter",
          "The compiler warns about it"
        ],
        "correctAnswer": "It decays to a pointer — sizeof becomes pointer size (8 bytes), array size is lost",
        "explanation": "C arrays decay to pointers. sizeof(arr) in a function returns 8 (pointer size), not the array size."
      }
    ]
  },
  {
    "note": "06 — std vector & Container Internals",
    "questions": [
      {
        "question": "What three pointers does std::vector store internally?",
        "options": [
          "head, tail, next",
          "begin, end (size), end of storage (capacity)",
          "first, last, current",
          "data, size, type"
        ],
        "correctAnswer": "begin, end (size), end of storage (capacity)",
        "explanation": "Note: vector stores begin pointer, end pointer (one past last element), and end-of-capacity pointer. sizeof(vector) = 24 bytes on 64-bit."
      },
      {
        "question": "What is the growth factor used by most std::vector implementations?",
        "options": [
          "1.5x (MSVC) or 2x (GCC/Clang)",
          "Always 2x",
          "Always 1.5x",
          "Square of current size"
        ],
        "correctAnswer": "1.5x (MSVC) or 2x (GCC/Clang)",
        "explanation": "Note: GCC/libc++ use 2x; MSVC uses 1.5x. Must be exponential for amortized O(1) push_back."
      }
    ]
  },
  {
    "note": "07 — std map & unordered_map",
    "questions": [
      {
        "question": "What data structure does std::map use internally?",
        "options": [
          "Hash table",
          "Red-black tree (self-balancing binary search tree)",
          "Sorted array",
          "Linked list"
        ],
        "correctAnswer": "Red-black tree (self-balancing binary search tree)",
        "explanation": "std::map is ordered and uses a balanced BST. O(log n) for all operations."
      },
      {
        "question": "What data structure does std::unordered_map use?",
        "options": [
          "Red-black tree",
          "Hash table with chaining",
          "Sorted array",
          "B-tree"
        ],
        "correctAnswer": "Hash table with chaining",
        "explanation": "std::unordered_map uses hashing. O(1) average, O(n) worst case for lookups."
      }
    ]
  },
  {
    "note": "08 — Iterators & Algorithms",
    "questions": [
      {
        "question": "What are the five iterator categories in C++?",
        "correctAnswer": "Input, Output, Forward, Bidirectional, Random Access",
        "acceptableAnswers": [
          "input output forward bidirectional random access",
          "Input Output Forward Bidirectional RandomAccess"
        ],
        "explanation": "Note lists five iterator categories: input (read-once), output (write-once), forward (multi-pass), bidirectional (--), random access ([] and +n)."
      },
      {
        "question": "What does std::sort require from its iterators?",
        "options": [
          "Forward iterators",
          "Bidirectional iterators",
          "Random access iterators",
          "Input iterators"
        ],
        "correctAnswer": "Random access iterators",
        "explanation": "std::sort requires random access iterators because it needs O(1) element access for partitioning."
      }
    ]
  },
  {
    "note": "09 — std span (C++20)",
    "questions": [
      {
        "question": "What is std::span?",
        "options": [
          "A heap-allocated array",
          "A non-owning view over contiguous data — pointer + size, zero allocation",
          "A C++23 container",
          "A thread-safe wrapper"
        ],
        "correctAnswer": "A non-owning view over contiguous data — pointer + size, zero allocation",
        "explanation": "Note: 'span is a non-owning view over a contiguous sequence. It stores exactly a pointer and a size. No allocation.'"
      },
      {
        "question": "What is the difference between std::span<T> and std::span<T, N>?",
        "options": [
          "No difference",
          "span<T> has dynamic extent (size stored at runtime, 16 bytes); span<T,N> has static extent (size in type, 8 bytes)",
          "span<T,N> is slower",
          "span<T> is deprecated"
        ],
        "correctAnswer": "span<T> has dynamic extent (size stored at runtime, 16 bytes); span<T,N> has static extent (size in type, 8 bytes)",
        "explanation": "Static-extent spans store only the pointer — the size is a compile-time constant, saving 8 bytes."
      }
    ]
  },
  {
    "note": "10 — std filesystem",
    "questions": [
      {
        "question": "What does std::filesystem::path::stem() return for 'archive.tar.gz'?",
        "options": [
          "archive",
          "archive.tar",
          "archive.tar.gz",
          "tar.gz"
        ],
        "correctAnswer": "archive.tar",
        "explanation": "stem() removes only the last extension. 'archive.tar.gz' → stem = 'archive.tar', extension = '.gz'."
      },
      {
        "question": "What does fs::create_directories() do?",
        "options": [
          "Creates only the last directory",
          "Creates all intermediate directories (like mkdir -p)",
          "Deletes directories",
          "Lists directory contents"
        ],
        "correctAnswer": "Creates all intermediate directories (like mkdir -p)",
        "explanation": "create_directories creates the entire path, including any missing parent directories."
      }
    ]
  },
  {
    "note": "11 — std deque, set & Container Adaptors",
    "questions": [
      {
        "question": "How does std::deque achieve O(1) push_front and push_back?",
        "options": [
          "Linked list of elements",
          "Array of pointers to fixed-size blocks (chunked array)",
          "Red-black tree",
          "Single contiguous array like vector"
        ],
        "correctAnswer": "Array of pointers to fixed-size blocks (chunked array)",
        "explanation": "deque uses a chunked array: a map of pointers to fixed-size blocks. Adding at either end allocates a new block as needed."
      },
      {
        "question": "Which container adaptor provides FIFO (first-in-first-out) behavior?",
        "options": [
          "std::stack",
          "std::queue",
          "std::priority_queue",
          "std::deque"
        ],
        "correctAnswer": "std::queue",
        "explanation": "std::queue adapts a container (default deque) to provide FIFO: push at back, pop from front."
      }
    ]
  },
  {
    "note": "12 — std pmr (C++17)",
    "questions": [
      {
        "question": "What is the key difference between std::vector<int> and std::pmr::vector<int>?",
        "options": [
          "No difference",
          "pmr::vector uses a runtime polymorphic allocator — same type regardless of where memory comes from; regular vector's allocator is a template parameter",
          "pmr::vector is faster",
          "pmr::vector is on the stack"
        ],
        "correctAnswer": "pmr::vector uses a runtime polymorphic allocator — same type regardless of where memory comes from; regular vector's allocator is a template parameter",
        "explanation": "Note: pmr decouples the allocator from the type. All pmr::vector<int> instances share the same type."
      },
      {
        "question": "What is a monotonic_buffer_resource?",
        "options": [
          "A thread-safe pool",
          "An arena allocator — pre-allocated buffer, bump-pointer allocation, freed all at once on destruction",
          "A garbage collector",
          "A shared memory region"
        ],
        "correctAnswer": "An arena allocator — pre-allocated buffer, bump-pointer allocation, freed all at once on destruction",
        "explanation": "monotonic_buffer_resource is a bump allocator. Individual deallocations are ignored; the entire buffer is reclaimed when the resource is destroyed."
      }
    ]
  },
  {
    "note": "01 — Inheritance & Virtual Functions",
    "questions": [
      {
        "question": "What does the virtual keyword enable?",
        "options": [
          "Faster function calls",
          "Runtime dispatch — the function called depends on the actual object type, not the pointer type",
          "Compile-time dispatch",
          "Automatic memory management"
        ],
        "correctAnswer": "Runtime dispatch — the function called depends on the actual object type, not the pointer type",
        "explanation": "Note: 'virtual tells the compiler: Don't decide which function to call based on the pointer type. Look at the actual object at runtime.'"
      },
      {
        "question": "What does override do and why should you always use it?",
        "options": [
          "Makes the function faster",
          "Compiler verifies the function actually overrides a base class virtual function — catches typos and signature mismatches",
          "It's required by the standard",
          "It prevents the function from being called"
        ],
        "correctAnswer": "Compiler verifies the function actually overrides a base class virtual function — catches typos and signature mismatches",
        "explanation": "Note: 'Always use override. It catches typos, wrong parameter types, wrong const qualifiers, and missing virtual in base.'"
      },
      {
        "question": "Why must a base class with virtual functions have a virtual destructor?",
        "options": [
          "To make the class polymorphic",
          "So delete through a base pointer calls the correct derived destructor chain",
          "It's a style convention",
          "To enable RTTI"
        ],
        "correctAnswer": "So delete through a base pointer calls the correct derived destructor chain",
        "explanation": "Note: 'If a class has ANY virtual function, make its destructor virtual. Without it, delete through base pointer only calls base destructor — leak.'"
      }
    ]
  },
  {
    "note": "02 — Lambdas & Function Objects",
    "questions": [
      {
        "question": "What does the capture list [] in a lambda control?",
        "options": [
          "Return type",
          "Which external variables the lambda can access and how (by value or reference)",
          "Parameter types",
          "The lambda's name"
        ],
        "correctAnswer": "Which external variables the lambda can access and how (by value or reference)",
        "explanation": "Note: 'The capture list controls which variables from the enclosing scope the lambda can access.' [=] copies, [&] references."
      },
      {
        "question": "What is the danger of [&] capture in asynchronous lambdas?",
        "options": [
          "It's slower",
          "Captured references may dangle if the lambda outlives the referenced variables",
          "It doesn't compile",
          "It causes memory leaks"
        ],
        "correctAnswer": "Captured references may dangle if the lambda outlives the referenced variables",
        "explanation": "Note warns: 'Capturing by Reference Is Dangerous. If the referenced variable is destroyed before the lambda runs, you have a dangling reference.'"
      }
    ]
  },
  {
    "note": "03 — C++ Casting & RTTI",
    "questions": [
      {
        "question": "Which cast performs a safe, runtime-checked downcast?",
        "options": [
          "static_cast",
          "dynamic_cast",
          "const_cast",
          "reinterpret_cast"
        ],
        "correctAnswer": "dynamic_cast",
        "explanation": "Note: 'dynamic_cast checks at runtime whether the cast is valid by walking the class hierarchy. Returns nullptr (pointer) or throws (reference) on failure.'"
      },
      {
        "question": "What does static_cast do for downcasts?",
        "options": [
          "Runtime check",
          "No runtime check — compiles silently, undefined behavior if wrong type",
          "Throws on failure",
          "Returns nullptr on failure"
        ],
        "correctAnswer": "No runtime check — compiles silently, undefined behavior if wrong type",
        "explanation": "Note warns: 'static_cast downcasts are dangerous. No runtime check — if base_ptr is actually Base, not Derived, undefined behavior.'"
      }
    ]
  },
  {
    "note": "04 — Operator Overloading",
    "questions": [
      {
        "question": "Which operators MUST be member functions (not non-member)?",
        "options": [
          "+, -, *",
          "=, [], (), ->",
          "==, !=, <",
          ">>, <<"
        ],
        "correctAnswer": "=, [], (), ->",
        "explanation": "Note: 'The rule: =, [], (), -> MUST be members. Others typically non-member for symmetry.'"
      },
      {
        "question": "What does the spaceship operator <=> (C++20) generate?",
        "options": [
          "Just ==",
          "All six comparison operators: <, <=, >, >=, ==, !=",
          "Just < and >",
          "Only ordering, not equality"
        ],
        "correctAnswer": "All six comparison operators: <, <=, >, >=, ==, !=",
        "explanation": "Note: 'One operator gives you ALL comparisons automatically. Generates ==, !=, <, <=, >, >= from comparing members.'"
      }
    ]
  },
  {
    "note": "05 — Templates & Concepts",
    "questions": [
      {
        "question": "When are templates instantiated?",
        "options": [
          "At link time",
          "At compile time, at the point of use — the compiler generates a separate copy for each type",
          "At runtime on first call",
          "During preprocessing"
        ],
        "correctAnswer": "At compile time, at the point of use — the compiler generates a separate copy for each type",
        "explanation": "Note: 'Templates are instantiated at the point of use. The compiler needs the full definition visible.'"
      },
      {
        "question": "What problem do C++20 concepts solve?",
        "options": [
          "Slow compilation",
          "Incomprehensible template error messages — concepts produce clean errors at the call site",
          "Binary size",
          "Runtime performance"
        ],
        "correctAnswer": "Incomprehensible template error messages — concepts produce clean errors at the call site",
        "explanation": "Note: 'The biggest problem with templates has always been error messages. Pass the wrong type and get 300 lines of trace. Concepts fix this.'"
      }
    ]
  },
  {
    "note": "01 — Move Semantics & Perfect Forwarding",
    "questions": [
      {
        "question": "What does std::move actually do?",
        "options": [
          "Moves the object",
          "Casts its argument to an rvalue reference, enabling move semantics",
          "Deletes the original object",
          "Copies the object efficiently"
        ],
        "correctAnswer": "Casts its argument to an rvalue reference, enabling move semantics",
        "explanation": "Note: 'std::move is a cast. It doesn't move anything — it casts to T&&, enabling the move constructor to be selected.'"
      },
      {
        "question": "What is the difference between std::move and std::forward?",
        "options": [
          "No difference",
          "move unconditionally casts to rvalue; forward conditionally casts based on template argument deduction",
          "forward is slower",
          "move is for unique_ptr; forward is for shared_ptr"
        ],
        "correctAnswer": "move unconditionally casts to rvalue; forward conditionally casts based on template argument deduction",
        "explanation": "Note: move always casts to rvalue reference. forward preserves the value category (lvalue stays lvalue)."
      }
    ]
  },
  {
    "note": "02 — constexpr, consteval & constinit",
    "questions": [
      {
        "question": "What is the key difference between const and constexpr?",
        "options": [
          "No difference",
          "const is a runtime promise (won't modify); constexpr guarantees compile-time evaluation",
          "const is faster",
          "constexpr is deprecated"
        ],
        "correctAnswer": "const is a runtime promise (won't modify); constexpr guarantees compile-time evaluation",
        "explanation": "Note: 'const promises I won't write to this. constexpr guarantees this value is known at compile time.'"
      },
      {
        "question": "What does consteval (C++20) enforce?",
        "options": [
          "Compile-time or runtime evaluation",
          "MUST be evaluated at compile time — error if called with runtime arguments",
          "Runtime only",
          "Lazy evaluation"
        ],
        "correctAnswer": "MUST be evaluated at compile time — error if called with runtime arguments",
        "explanation": "Note: 'consteval creates an immediate function — every call MUST produce a compile-time constant.'"
      }
    ]
  },
  {
    "note": "03 — noexcept & Move Semantics",
    "questions": [
      {
        "question": "Why does marking a move constructor noexcept improve std::vector performance?",
        "options": [
          "It doesn't",
          "vector uses move_if_noexcept: noexcept move = pointer swap; non-noexcept = forced copy on reallocation for exception safety",
          "noexcept enables SIMD moves",
          "It reduces binary size only"
        ],
        "correctAnswer": "vector uses move_if_noexcept: noexcept move = pointer swap; non-noexcept = forced copy on reallocation for exception safety",
        "explanation": "Note: 'vector checks noexcept. Yes → move (pointer swap, ~3 cycles). No → copy (deep copy, thousands of cycles).'"
      }
    ]
  },
  {
    "note": "04 — std variant, optional & any",
    "questions": [
      {
        "question": "What is the key difference between std::variant and a C union?",
        "options": [
          "No difference",
          "variant has a discriminator tracking which alternative is active — type-safe; C unions don't track active member",
          "variant is slower",
          "variant is on the heap"
        ],
        "correctAnswer": "variant has a discriminator tracking which alternative is active — type-safe; C unions don't track active member",
        "explanation": "Note: 'variant adds a discriminator tracking the active alternative. A C union doesn't track which member is active.'"
      },
      {
        "question": "Does std::optional allocate on the heap?",
        "options": [
          "Yes, always",
          "No — stores the value inline, sizeof approximately sizeof(T) + alignment padding",
          "Only for large types",
          "It depends on the compiler"
        ],
        "correctAnswer": "No — stores the value inline, sizeof approximately sizeof(T) + alignment padding",
        "explanation": "Note: 'optional is NOT a pointer. No heap allocation. Stores the value inline in its own footprint.'"
      }
    ]
  },
  {
    "note": "05 — Coroutines (C++20)",
    "questions": [
      {
        "question": "What are the three keywords that identify C++20 coroutines?",
        "correctAnswer": "co_await, co_yield, co_return",
        "acceptableAnswers": [
          "co_await co_yield co_return",
          "co_await, co_yield, co_return"
        ],
        "explanation": "Note discusses co_await (suspend and resume), co_yield (produce a value and suspend), and co_return (complete the coroutine)."
      }
    ]
  },
  {
    "note": "06 — std ranges (C++20)",
    "questions": [
      {
        "question": "What is the key advantage of ranges over traditional iterator-pair algorithms?",
        "options": [
          "Faster execution",
          "Composable pipelines with lazy evaluation — filter, transform, take chain without intermediate allocations",
          "Smaller binary",
          "Better error messages"
        ],
        "correctAnswer": "Composable pipelines with lazy evaluation — filter, transform, take chain without intermediate allocations",
        "explanation": "Note: 'Ranges compose with | (pipe). filter | transform | take — zero intermediate allocations, lazy evaluation.'"
      }
    ]
  },
  {
    "note": "07 — C++23 Overview",
    "questions": [
      {
        "question": "What does deducing this (C++23) eliminate?",
        "options": [
          "Virtual functions",
          "The CRTP pattern — no more static_cast<Derived*>(this)",
          "Templates",
          "Move semantics"
        ],
        "correctAnswer": "The CRTP pattern — no more static_cast<Derived*>(this)",
        "explanation": "Note: 'Deducing this kills the CRTP pattern. void f(this Self&& self) deduces the derived type automatically.'"
      }
    ]
  },
  {
    "note": "01 — C++ vs UE",
    "questions": [
      {
        "question": "Why does UE disable C++ exceptions?",
        "options": [
          "They're too slow",
          "Performance predictability, code size, and console platform constraints",
          "They don't work on Windows",
          "They're deprecated"
        ],
        "correctAnswer": "Performance predictability, code size, and console platform constraints",
        "explanation": "Note: UE replaces exceptions with check()/ensure()/verify() macros that are more predictable with smaller codegen impact."
      }
    ]
  },
  {
    "note": "02 — From Source to Running",
    "questions": [
      {
        "question": "What are the four stages of C++ compilation in order?",
        "correctAnswer": "Preprocessor, Compiler, Assembler, Linker",
        "acceptableAnswers": [
          "preprocessor compiler assembler linker",
          "Preprocessor → Compiler → Assembler → Linker"
        ],
        "explanation": "Note shows pipeline: .cpp → preprocessor (.i) → compiler (.s) → assembler (.o) → linker (.exe)."
      }
    ]
  },
  {
    "note": "03 — Modules (C++20)",
    "questions": [
      {
        "question": "What is the primary advantage of C++20 modules over #include?",
        "options": [
          "Faster runtime",
          "Dramatically faster compilation — modules are pre-compiled, not text-pasted",
          "Smaller binary",
          "Easier to write"
        ],
        "correctAnswer": "Dramatically faster compilation — modules are pre-compiled, not text-pasted",
        "explanation": "Note: modules avoid the repeated text parsing of headers. 'import std;' replaces including the entire standard library."
      }
    ]
  },
  {
    "note": "04 — Exceptions & Error Handling",
    "questions": [
      {
        "question": "What is stack unwinding?",
        "options": [
          "A compiler optimization",
          "When an exception is thrown, destructors of all local objects are called in reverse order as the stack unwinds to find a catch handler",
          "Memory cleanup",
          "Function inlining"
        ],
        "correctAnswer": "When an exception is thrown, destructors of all local objects are called in reverse order as the stack unwinds to find a catch handler",
        "explanation": "Note: stack unwinding is the process of destroying local objects as the runtime searches up the call stack for a matching catch block."
      }
    ]
  },
  {
    "note": "05 — CMake & Build Systems",
    "questions": [
      {
        "question": "What is CMake?",
        "options": [
          "A compiler",
          "A build system generator — it generates native build files (Makefiles, VS solutions) from a platform-independent script",
          "A linker",
          "A package manager"
        ],
        "correctAnswer": "A build system generator — it generates native build files (Makefiles, VS solutions) from a platform-independent script",
        "explanation": "Note: CMake is a meta-build system. It reads CMakeLists.txt and generates platform-specific build files."
      }
    ]
  }
]